<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Abdallah GDC Hamade "Azrael" - Retro Résumé</title>
  <link href="https://fonts.googleapis.com/css2?family=VT323&display=swap" rel="stylesheet">
  <style>
    body {
      background:#0a0a0a;
      color:#fff;
      font-family:'VT323', monospace;
      margin:2rem;
    }
    .neon {
      color:#0ff;
      text-shadow:0 0 5px #0ff, 0 0 10px #0ff;
    }
  .hero {
  position: fixed;
  bottom: 20px;
  left: 20px;
  width: 96px;
  height: 96px;
  background: url('https://raw.githubusercontent.com/AzzyYourLocalCoffeeGod/Retro-Resume/main/Azzy%20pixel%20sprite.png') no-repeat;
  background-size: 576px 96px;
  image-rendering: pixelated;
  animation: walkRight 0.8s steps(6) infinite;
  z-index: 10;
}
    @keyframes walkRight {
      from { background-position: 0 0; }
      to { background-position: -576px 0; }
    }
    @keyframes walkLeft {
      from { background-position: -576px 0; }
      to { background-position: 0 0; }
    }
    .walk-left {
      animation-name: walkLeft;
    }
  </style>
</head>
<body>
  <div class="hero"></div>

  <header class="neon"><h1>Abdallah GDC Hamade</h1></header>
  <section>
    <h2>About</h2>
    <p>Text here</p>
  </section>

  <!-- Typing effect -->
  <script>
    const header = document.querySelector('header h1');
    const text = header.textContent;
    header.textContent = '';
    let i = 0;
    function type() {
      if (i < text.length) {
        header.textContent += text.charAt(i);
        i++;
        setTimeout(type, 100);
      }
    }
    type();
  </script>

  <!-- Walking direction change -->
  <script>
   window.addEventListener('load', () => {
    const hero = document.querySelector('.hero');
    let direction = 'right';

    if (hero) {  // safety check
      hero.addEventListener('animationiteration', () => {
        if (direction === 'right') {
          hero.classList.add('walk-left');
          direction = 'left';
        } else {
          hero.classList.remove('walk-left');
          direction = 'right';
        }
      });
    }
   });
  </script>
</body>
</html>