** start of script.js **

function uniteUnique(...arrs) {
const result = [];
arrs.flat().forEach(num => {
  if (!result.includes(num)){
    result.push(num);
  }
});
return result;
}

** end of script.js **

