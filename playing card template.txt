<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Playing Cards</title>
  <style>
    body {
      margin: 0;
      padding: 2rem;
      background: #0a0015;
      font-family: 'Segoe UI', sans-serif;
      min-height: 100vh;
      display: flex;
      justify-content: center;
    }

    #playing-cards {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 20px;
      max-width: 1400px;
    }

    .card {
      width: 100px;
      height: 140px;
      background: white;
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.5);
      position: relative;
      display: flex;
      justify-content: space-between;
      padding: 8px;
      font-size: 1.8rem;
      font-weight: bold;
      overflow: hidden;
    }

    .left   { align-self: flex-start; }
    .middle { 
      display: flex;
      align-self: center; flex-direction: column; text-align: center; }
    .right  { align-self: flex-end; transform: rotate(180deg); }

    /* Suit colors */
    .spades .left, .spades .right { color: black; }
    .hearts .left, .hearts .right { color: red; }
    .clubs .left, .clubs .right   { color: black; }
    .diamonds .left, .diamonds .right { color: red; }

    .middle .number {
      font-size: 3rem;
      line-height: 1;
    }
  </style>
</head>
<body>

  <main id="playing-cards">
    <!-- Ace of Spades example -->
    <div class="card spades">
      <div class="left">A</div>
      <div class="middle">
        <div class="number">A</div>
        <div>♠</div>
      </div>
      <div class="right">A</div>
    </div>

    <!-- Two of Hearts -->
    <div class="card hearts">
      <div class="left">2</div>
      <div class="middle">
        <div>♥</div>
        <div>♥</div>
      </div>
      <div class="right">2</div>
    </div>

    <!-- Three of Clubs -->
    <div class="card clubs">
      <div class="left">3</div>
      <div class="middle">
        <div>♣</div>
        <div>♣</div>
        <div>♣</div>
      </div>
      <div class="right">3</div>
    </div>

    <!-- Add as many more as you want — FCC only needs 3 -->
  </main>

</body>
</html>

